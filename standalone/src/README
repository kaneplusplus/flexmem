Our makefile uses a variable called PREFIX to set up the installation path.
The default value is /usr/local. See the examples below...

Quick example:

make
XMEM_HOME=$(pwd) ./xmem <program> [program arguments]


Or, install into the default location of /usr/local/:

make
make install
xmem <program>
